import React from 'react';

import { Wrapper } from './styles';
import Button from 'components/Button';

const Searchbar = () => (
  <Wrapper>
    <Button weight="normal">Search for places</Button>
  </Wrapper>
);

export default Searchbar;
